bool is_sorted(array data) {
    int i
    i := 1
    while ((i < sizeof(data))) {
        if ((data[(i - 1)] > data[i])) {
            return false
        }
        i := (i + 1)
    }
    return true
}

int main() {
    int n
    n := input()
    if ((n <= 0)) {
        print("Illegal array size...\n")
        return -1
    }
    array data[n]
    int x
    for (x : data) {
        x := input()
    }

    if (is_sorted(data)) {
        print("The input array is sorted!\n")
    } else {
        print("The input array is NOT sorted!\n")
    }
    return 0
}
